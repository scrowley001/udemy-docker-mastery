without a config located at ~/.kube/ you would need to specify the below parameters with each kubectl command
--client-key
--client-certificate
--certificate-authority
--server

To use a config file not located at the default location use the '--kubeconfig' flag
k config --kubeconfig=/root/my-kube-config use-context research
